# Virtual env
venv/
.venv/
env/

# Secrets — real values live in .env (not committed); .env.example is the template
.env

# Large generated artifacts — never commit these (model weights live on Hugging Face Hub,
# audio dataset is 23G+44G — share via HF Datasets / shared drive, not GitHub)
checkpoints/
datasets/
kaskor-asr-*-export/
*.npy
*.safetensors
*.bin
*.pt
*.ckpt

# Logs / run state
*_log.txt
*_err.txt
eval_history.json

# Python
__pycache__/
*.pyc
*.egg-info/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
